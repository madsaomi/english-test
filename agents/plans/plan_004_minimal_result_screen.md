# 📐 Implementation Plan: Бумажный формат теста + приёмная (TASK-006)

- **ID плана:** PLAN-004
- **Дата создания:** 2026-09-22
- **Статус:** ✅ COMPLETED (Выполнен)
- **Цель:** Сайт показывает только процесс как на бумажном тесте: каталог → вопросы с вариантами ответов → форма «ФИО + телефон» → «Спасибо! Подойдите к администратору». Результат на сайте НЕ отображается: сотрудник получает его в Telegram-боте (карточка + детальный результат двумя сообщениями).

---

## 📌 Чек-лист реализации

- [x] **Шаг 1:** `static/index.html` — экран после теста оставить максимально пустым:
  - Удалить: бейдж уровня CEFR + название, описание уровня (`#result-summary-text`), пилюли «Балл/Точность/Время», блоки «Детализация по навыкам», «Темы с неточностями», «Индивидуальные рекомендации», весь «Подробный разбор каждого вопроса» (`#review-section`).
  - Оставить: шапку «🎯 Тестирование завершено» + форму ФИО/Телефон/Telegram + кнопку отправки.
  - Убрать старый текст «Получите разбор ошибок, советы и сохраните сертификат...» → заменить на «Оставьте ваши контакты — результат получит администратор».
  - Блок успеха (`#tg-success-message`) → заголовок **«Спасибо! Подойдите к администратору.»**
- [x] **Шаг 2:** `static/js/app.js` — в `renderResult` удалить отрисовку навыков/слабых тем/рекомендаций/разбора и вызов `renderReview`; удалить функцию `renderReview` и её переменные. Отрисовка результатов (уровень/балл) на сайте не выполняется вообще — данные по-прежнему уходят в бота.
- [x] **Шаг 3:** Документация: `agents/tasks/active_task.md` (TASK-006), `agents/STATUS.md`, журнал сессии 11, план → COMPLETED.

---

## 🧪 План верификации
1. `python agents/tools/check_integrity.py` → 100% OK.
2. `python agents/testing/test_api_http.py` → ALL PASSED (бэкенд и Telegram-уведомления не меняются).
3. Ручной smoke: полный тест → после ответов форма ФИО/телефон → «Спасибо! Подойдите к администратору»; в демо-логе бота карточка + детальный результат.

## 📁 Затрагиваемые файлы
- `static/index.html`
- `static/js/app.js`
- `agents/tasks/active_task.md`, `agents/STATUS.md`, `agents/history/` (новый журнал)

## ⚠️ Риски и меры
- Только фронтенд; бэкенд (CAT, lead store, `/api/export/leads`, Telegram-уведомления) не трогаем.
- Удаление DOM-блоков при сохранении null-guarded JS исключает ошибки рендера.
- Кандидат может захотеть увидеть свой уровень → на будущее можно добавить «показать результат» опционально, но не в этот план.