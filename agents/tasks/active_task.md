# 📋 Активная задача: TASK-005 — Заявки в приёмную (Reception Leads)

**Статус:** 🟢 COMPLETED (Завершена)  
**Дата создания:** 2026-09-21  
**Дата завершения:** 2026-09-22  
**Исполнитель:** opencode (opencode/big-pickle)  

---

## 🎯 Цель задачи
Перевести рабочий процесс школы на «цифровую приёмную»: кандидат проходит тест на сайте, вводит ФИО и телефон (обязательно), сотрудник получает в Telegram-боте компактную карточку заявки (Вариант A) и следом — полные результаты кандидата. Раньше проверка шла на бумаге вручную.

---

## 📌 Чек-лист выполнения:

- [x] **Шаг 1:** Телефон обязателен во фронте (`static/index.html`, `required`); поле Telegram осталось необязательным.
- [x] **Шаг 2:** Новый модуль `backend/lead_store.py` — `LeadRecord` + персистентность в `data/leads.json` (переживает рестарт и GC).
- [x] **Шаг 3:** Бот (`backend/telegram_bot.py`): `format_compact_lead_card` (Вариант A), `send_admin_lead_notification` — ДВА сообщения сотруднику (карточка → детальный результат), `send_student_full_result` кандидату.
- [x] **Шаг 4:** `backend/main.py`: серверная валидация имени/телефона (400), сохранение лида в store до отправки, `/api/export/leads` переведён на lead store; `.gitignore` дополнен `data/`.
- [x] **Шаг 5:** Тесты `agents/testing/test_api_http.py`: телефон обязателен (400/422), лид сохраняется и экспортируется, формат карточки (ФИО/уровень/телефон/дата).
- [x] **Шаг 6:** Документация: `active_task.md` (TASK-005), `STATUS.md`, журнал сессии 10, план PLAN-003 → COMPLETED.

---

## 🏁 Результаты:
Кандидат вводит имя + телефон (оба обязательны). Сотруднику в `ADMIN_CHAT_ID` приходят подряд: компактная карточка (Имя / Уровень / Телефон в код-блоке / Дата) и вторым сообщением — полный детальный результат (навыки, слабые темы, рекомендации). Лиды хранятся в `data/leads.json` и доступны через `/api/export/leads` даже после рестарта сервера. Все тесты PASSED, включая новые HTTP-кейсы.