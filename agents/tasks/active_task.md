# 📋 Активная задача: TASK-007 — Минималистичный интерфейс без подсказок

**Статус:** 🟢 COMPLETED (Завершена)  
**Дата создания:** 2026-09-22  
**Дата завершения:** 2026-09-22  
**Исполнитель:** opencode (opencode/big-pickle)  

---

## 🎯 Цель задачи
Убрать с экрана теста все «подсказки» (название теста, уровень сложности, категорию, тему вопроса) и переработать интерфейс в минималистичный, чистый вид: без маркетингового шума и отвлекающих элементов, в духе TASK-006 («бумажный тест»).

---

## 📌 Чек-лист выполнения:

- [x] **Шаг 1:** `static/index.html` — удалены подсказки: `#q-test-title` (название теста в шапке вопроса), `#q-level-badge` / `#current-eval-label` («B1 (Intermediate)»), блок `#question-meta` с `#q-category` («Grammar») и `#q-topic` («Present Perfect vs Past Simple»), плашка «💡 Подсказка...», тег `#result-test-name` на экране результата.
- [x] **Шаг 2:** `static/index.html` — минимализм: из шапки убран `header-status` («AI Adaptive Engine»), удалены `hero-pill` («✨ Компьютерное адаптивное тестирование (CEFR)») и сетка фич `features-grid`; подзаголовок сокращён до одной строки; заголовок каталога — «Выберите тест»; кнопка старта — «Начать тест» (статично), `test-meta-hint` удалён; экран результата — единый заголовок «Тестирование завершено» + форма.
- [x] **Шаг 3:** `static/js/app.js` — удалены лакапы и все записи для `qTestTitle`, `qLevelBadge`, `currentEvalLabel`, `qCategory`, `qTopic`, `resultTestName`, `testMetaHint`; удалена переменная `currentTestTitle`; карточки каталога упрощены (иконка + название + описание + время/вопросы, без бейджей уровня/режима); кнопка старта → «Начать тест».
- [x] **Шаг 4:** `static/css/style.css` — компактнее hero (padding, размеры заголовка/подзаголовка), заголовок каталога и карточки тестов тоньше, заголовок результата отцентрован. Мёртвые классы (`.q-level-badge`, `.category-tag` и т.п.) намеренно оставлены — вычистка отдельной задачей.
- [x] **Шаг 5:** Верификация: `check_integrity.py` → 100% OK; `test_api_http.py` → 10/10 PASSED; `test_simulation` → 3/3; live smoke — ни одного удалённого идентификатора в HTML/JS, «Пройдите тест английского» и «Начать тест» на месте.
- [x] **Шаг 6:** Документация: `active_task.md` (TASK-007), `STATUS.md`, журнал сессии 12, план PLAN-005 → COMPLETED.

---

## 🏁 Результаты:
На экране вопроса осталось только функциональное ядро: счётчик «Вопрос N из ~M», таймер, прогресс-бар, текст вопроса и 4 варианта ответа — никаких подсказок об уровне/категории/теме. Велком-экран чистый: заголовок, одна строка подзаголовка, выбор теста, кнопка «Начать тест». Бэкенд не менялся. Все тесты PASSED.